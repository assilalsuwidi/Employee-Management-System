# الإصلاح (سلسلة التوريد): كانت كل الحزم مثبّتة بـ ">=" فقط بلا سقف أو
# تثبيت دقيق، فأي بناء جديد للصورة قد يجلب إصدارًا مستقبليًا غير
# مُختبَر (أو معطوبًا) دون قصد. الإصدارات أدناه مثبّتة بدقة (==) وهي ما
# جرى اختباره فعليًا لهذا المشروع؛ الترقية تتم عمدًا (تحديث الرقم هنا +
# إعادة تشغيل الاختبارات وCI)، وليس تلقائيًا عند كل بناء. `safety check`
# في CI يبقى يراقب الإصدارات المثبّتة هذه بحثًا عن CVEs معروفة.
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1
Flask-Limiter==3.7.0
PyMySQL==1.1.1
python-dotenv==1.0.1
bcrypt==4.1.3
pytest==8.2.2
pytest-flask==1.3.0
flake8==7.1.0
bandit==1.7.9
safety==3.2.3
flask-talisman==1.1.0
Pillow==10.4.0
gunicorn==22.0.0
redis==5.0.7
